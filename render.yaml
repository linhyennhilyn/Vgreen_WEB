services:
  - type: web
    name: vgreen-backend
    runtime: node
    buildCommand: cd backend && npm install
    startCommand: node backend/server.supabase.js
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false